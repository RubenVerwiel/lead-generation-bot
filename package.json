{
  "name": "lead-generation-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start-local": "set -a && . ./.env.local && set +a && vercel dev"
  }
}
